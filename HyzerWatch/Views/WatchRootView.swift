import SwiftUI
import HyzerKit

/// Watch app shell — placeholder for Story 7.1.
struct WatchRootView: View {
    var body: some View {
        Text("HyzerApp")
            .font(TypographyTokens.h3)
            .foregroundStyle(Color.textPrimary)
    }
}
